---
title: "Delete User Memory"
openapi: "DELETE /memories/delete_memory"
description: "API to delete memories"
---

import { TableOfContents } from '/snippets/table-of-contents.jsx'

<Tabs>
  <Tab title="API Request">
    ```bash
    curl -X 'DELETE' \
      'https://api.usecortex.ai/memories/delete_memory?tenant_id=tenant_123&memory_id=memory_123' \
      -H 'accept: application/json' \
      -H 'Authorization: Bearer YOUR_API_KEY'
    ```
  </Tab>
  <Tab title="TypeScript">
    ```ts
    const result = await client.memories.deleteMemory({
      tenant_id: "tenant_1234",
      memory_id: "memory_1234",
      sub_tenant_id: "sub_tenant_4567"
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # Async usage is similar, just use async_client and await
    result = client.memories.delete_Memory(
        tenant_id="tenant_1234",
        memory_id="memory_1234",
        sub_tenant_id="sub_tenant_4567"
    )
    ```
  </Tab>
</Tabs>

## Response Fields

| Field                 | Type    | Description                                                   |
| --------------------- | ------- | ------------------------------------------------------------- |
| `success`             | boolean | Indicates whether the operation was successful                |
| `user_memory_deleted` | boolean | Confirms whether the specific memory was successfully deleted |

## Parameters

| Parameter       | Type   | Required | Description                                                                                        |
| --------------- | ------ | -------- | -------------------------------------------------------------------------------------------------- |
| `tenant_id`     | string | Yes      | Primary organizational identifier                                                                  |
| `sub_tenant_id` | string | No       | Secondary organizational identifier; if omitted, defaults to the default sub-tenant for the tenant |
| `memory_id`     | string | Yes      | Unique identifier of the memory to delete                                                          |